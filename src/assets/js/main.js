/**
 * jQuerys document.ready function. Inside
 * this function you can call the properties
 * and methods returned from all modules located
 * within the /modules directory.
 */
$(function() {
	console.log(Example.title)
})
